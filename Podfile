source 'https://github.com/CocoaPods/Specs.git'
platform :ios, '11.0'

use_frameworks!

target 'Biblio' do
   pod 'Firebase/Core'
   pod 'Firebase/AdMob'
   pod 'SnapKit', '~> 4.0.0'
   pod 'fluid-slider'
   target  'BiblioTests' do
        inherit! :search_paths
        pod 'Firebase'
    end
end
